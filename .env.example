# â”€â”€â”€ App Parameters â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
MODE=dev                 # dev | prod

# â”€â”€â”€ OAuth client for your app â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
GOOGLE_CLIENT_ID=YOUR_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_CLIENT_SECRET
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# Local web server for OAuth callback
PORT=3000

# â”€â”€â”€ Target stream channel ID (the channel youâ€™re streaming on) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# REQUIRED: The canonical channel ID thatâ€™s streaming (looks like UCxxxxxxxxxxxxxxx)
TARGET_CHANNEL_ID="UC_yourMainChannelIDHere"

# Optional: If you run multiple streams and want to match by part of the title,
# set this. Otherwise the most recent live video is picked.
# TARGET_TITLE_MATCH=My Stream Title

# â”€â”€â”€ Target stream URL (URL of the channel you want to join) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
TARGET_LIVESTREAM_URL="LIVESTREAM_URL"

OPENAI_API_KEY=OPEN_API_KEY
OPENAI_MODEL=gpt-4o-mini
CHAT_MAX_CHARS=190   # keep <200; 190 is a nice buffer

# Owner check (pick one: the automatic isChatOwner flag usually suffices)
OWNER_CHANNEL_ID=OWNER_CHANNEL_ID   # optional fallback if needed

# â”€â”€â”€ Bot behavior â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# Command prefix for chat commands
COMMAND_PREFIX=!

# Auto announcements
AUTO_ANNOUNCEMENTS_MAX=5

# Custom commands
CUSTOM_COMMANDS_MAX=15

# Discord message size
DISCORD_MAX_CHARS=1990

# Polling fallback delay (ms) if API doesnâ€™t specify one (10000 = 10 seconds)
POLLING_FALLBACK_MS=10000

# API League Variables
APILEAGUE_API_KEY=APILEAGUE_API_KEY
RIDDLE_TIMEOUT=120000 # 120000 is 2 minutes

#STREET RACE
RACE_COOLDOWN_MS=20000      # 20 seconds
RACE_JOIN_WINDOW_MS=20000  # 100 seconds

# Discord transport (optional)
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN
# Optional CSV list of guild IDs; leave blank to allow all
DISCORD_ALLOWED_GUILD_IDS=
# Optional CSV list of channel IDs; leave blank to allow all
DISCORD_ALLOWED_CHANNEL_IDS=
# Restrict racing commands to these guildId:channelId pairs (comma separated)
DISCORD_RACING_CHANNELS=

# Optional CSV to disable modules globally
DISABLED_MODULES=weather

# Crypto game (CoinGecko-backed paper trading)
CRYPTO_ALLOWED_COINS=BTC,ETH,SOL,DOGE,LTC,BNB,XRP,ADA
CRYPTO_STARTING_CASH=1000
COINGECKO_TTL_MS=60000  # set 0 for no cache

# quota estimation
POLL_ESTIMATE_UNITS=5
YT_DAILY_QUOTA=10000        # tweak if YT raised your quota

# Semantic word game (OpenAI embeddings)
SEMANTIC_TARGET_WORD=apple




